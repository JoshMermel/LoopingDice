package com.joshmermelstein.loopoverplus

import junit.framework.TestCase

// Tests for various utilities around random level generation.

class RandomLevelTest : TestCase() {
    /* TODO(jmerm)
     * bicolor - right number of each color across several board sizes
     * bicolor (dynamic) - only one color other than fixed cells
     *
     * speckled (typical) - ensure right number of each
     * speckled (bandaged) - ensure no bandaged are speckled
     * speckled (dynamic) - ensure right number of each
     * speckled (enabled) - ensure right number of each
     * speckled (static) - ensure right number of each.
     *
     * columns typical
     * columns (static/dynamic/bandaged) - no black squares
     * columns (enabler) - no gold cells
     *
     * unique (typical)
     * unique (dynamic) different numbers of pips
     */
}